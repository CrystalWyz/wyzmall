spring:
  application:
    name: wyzmall-third-party
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        namespace: wyzmall-third-party
        group: dev
        file-extension: yml
        extension-configs:
          - data-id: oss.yml
            group: DEFAULT_GROUP
            refresh: true